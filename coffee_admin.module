<?php


function coffee_admin_coffee_commands($op) {
  $commands = array();

  // Basic example, for 1 result.
  $commands[] = array(
    'value' => 'devel/cache/clear',
    'label' => 'Clear Cache',
    'command' => ':clear cache',
  );

  return $commands;
}



?>